[package]
name = "expr-solver-bin"
version = "1.2.0"
edition = "2024"
authors = ["Albert Varaksin <albeva@me.com>"]
description = "Command-line mathematical expression evaluator with high-precision Decimal arithmetic and bytecode compilation"
license = "MIT"
readme = "../README.md"
repository = "https://github.com/albeva/expr-solver"
keywords = ["math", "expression", "parser", "evaluator", "calculator"]
categories = ["mathematics", "parser-implementations"]

[[bin]]
name = "expr-solver"
path = "src/main.rs"

[dependencies]
expr-solver-lib = { version = "1.2.0", path = "../lib", default-features = false, features = ["decimal-precision", "serialization", "printing"] }
clap = { version = "4.0", features = ["derive"] }
